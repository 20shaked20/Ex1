//
// Created by Shaked Levi on 26/10/2021.
//
#include <stdio.h>
#include "NumClass.h"

enum bool{true = 1,false = 0};

/// private helper method - reverse in recursion:
int reverse(int num,int tmp){
    if(n==0){
        return tmp;
    }
    tmp = (tmp*10) + (n%10);
    return (n/10, temp);

}

int isPalindrome(int num){

    if (num == reverse(num,0)){
        return true;
    }
    return false;
}

///private helper method.

int isArm(int num){

}

int isArmstrong(int num) { // arm 407 = 4^3 + 0^3 + 7^3

    if(num == isArm(num))
        return true;

    return isArmstrong()
}
